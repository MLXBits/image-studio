# Copy this file to .env (gitignored) and fill in your values.
#   cp .env.template .env
#
# Optional: only needed to build a signed Release locally. Automated releases
# run in CI and use repo secrets instead (see .github/workflows/release.yml):
DEVELOPMENT_TEAM=XXXXXXXXXX
