# macOS
.DS_Store
.AppleDouble
.LSOverride
*.icloud

# Xcode user state (machine/person-specific, never commit)
xcuserdata/
*.xcuserstate
*.xccheckout
*.moved-aside
*.pbxuser

# Xcode build output
build/
DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata

# CocoaPods
Pods/

# Carthage
Carthage/Build/

# Code-quality tool output
.jscpd-report/

# AI assistant / IDE tool state (machine-specific, never commit)
.claude/
.continue/
.cursor/
# AGENTS.md is the tracked, canonical agent guidance. CLAUDE.md is a local
# symlink to it so Claude Code picks it up automatically — create it with
# `ln -s AGENTS.md CLAUDE.md` (see README).
/CLAUDE.md

# Local secrets (Team ID, credentials) — never commit
.env
__pycache__/
